# Backend Tasks

**Owner:** Backend Engineer  
**Main responsibility:** Serve model predictions and Gemini explanations over HTTP so the frontend can render the dashboard from live JSON.

**Input (from ML):** `ml/artifacts/model.joblib`, `latest_features.csv`, `model_metadata.json`, `zip_metadata.csv`

**Deliverable (output contract → Frontend):** HTTP API returning JSON
