# MosquitoWatch NYC frontend

Next.js (App Router) dashboard. Mock ZIP forecasts live in `lib/mock-data.ts` / `lib/zips.json`.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). No Gemini API key is required for this mock.
